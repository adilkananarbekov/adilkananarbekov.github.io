# Google Analytics 4 — Measurement ID from GA: Admin → Data streams → Web → Measurement ID
# Copy to .env.local (not committed) for local testing; set the same in GitHub Actions secrets or Pages env if you use CI.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
